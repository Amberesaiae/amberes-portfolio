.vercel
.env*.local

# dependencies / build output
node_modules/
dist/

# OS / logs
.DS_Store
*.log

# Design refs (screenshots) — kept out of history, already ~22MB tracked
Claude outputs/

# Local-only asset sources / scratch (unreferenced by the site)
app/pics/
softwares/
svg/

# Wrangler local dev state (miniflare cache/r2 sqlite)
.wrangler/
