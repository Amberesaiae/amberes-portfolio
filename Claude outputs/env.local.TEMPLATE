# Save this as app/.env.local  (already covered by .gitignore's ".env*.local")
#
# ⚠ Use NEW keys, not the ones you pasted into the chat. Those two are now in a
# transcript and must be treated as public — revoke them and generate fresh
# ones. The same values go into Vercel → Settings → Environment Variables.

# https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-v1-PASTE_A_FRESH_KEY_HERE

# Any model slug OpenRouter accepts. Cheap and fast is right for this.
OPENROUTER_MODEL=google/gemini-2.0-flash-001

# Sent to OpenRouter for attribution. Use the deployed URL in production.
SITE_URL=http://localhost:3000

# Contact form — see .env.example for the email service options.
CONTACT_EMAIL=isaiahamber5@gmail.com
