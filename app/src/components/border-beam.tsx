/**
 * The focus beam. Re-export of the real package — see thinking-orbs.tsx for why
 * this is a shim rather than an implementation.
 */
export { BorderBeam } from 'border-beam';
export type { BorderBeamProps, BorderBeamColorVariant } from 'border-beam';
